<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.admin.fueltracker.MainActivity">

    <Button
        android:text="Cars"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/carsBtn"
        android:layout_marginTop="59dp"
        android:elevation="0dp"
        android:onClick="goToCarsActivity"
        android:layout_alignParentTop="true"
        android:layout_alignRight="@+id/fuelBtn"
        android:layout_alignEnd="@+id/fuelBtn"
        android:layout_alignLeft="@+id/fuelBtn"
        android:layout_alignStart="@+id/fuelBtn" />

    <Button
        android:id="@+id/fuelBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/carsBtn"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="12dp"
        android:onClick="goToFuelActivity"
        android:text="refueling" />

    <Button
        android:id="@+id/reportsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/fuelBtn"
        android:layout_alignLeft="@+id/fuelBtn"
        android:layout_alignRight="@+id/fuelBtn"
        android:layout_alignStart="@+id/fuelBtn"
        android:layout_below="@+id/fuelBtn"
        android:layout_marginTop="12dp"
        android:onClick="goToReportsActivity"
        android:text="Reports" />

    <Button
        android:id="@+id/mapsBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/reportsBtn"
        android:layout_alignLeft="@+id/reportsBtn"
        android:layout_alignRight="@+id/reportsBtn"
        android:layout_alignStart="@+id/reportsBtn"
        android:layout_below="@+id/reportsBtn"
        android:layout_marginTop="15dp"
        android:onClick="goToMapsActivity"
        android:text="MAP"/>

</RelativeLayout>
